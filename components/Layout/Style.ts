import styled from '@emotion/styled'
import { SCREEN_SIZE } from 'constatns/screen'

export const PageLayoutStyle = styled.main`
  display: flex;
  overflow: hidden;
  padding: 20px;
  margin: 0 auto;
  max-width: ${SCREEN_SIZE.lg};
  width: 100%;
  height: 100%;
`
